{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "outDir": "./out-tsc/app",
    "types": [
      "node",
      "@angular/localize"
    ]
  },
  "files": [
    "src/main.ts" // Удалите "src/main.server.ts" и "server.ts", если они не нужны
  ],
  "include": [
    "src/**/*.d.ts",
    "src/**/*.ts" // Добавьте .ts файлы, чтобы включить все TypeScript файлы
  ]
}
